{
  "name": "agent-plugin",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "verify:release": "node scripts/verify-release.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "yaml": "2.9.0"
  },
  "overrides": {
    "fast-uri": "3.1.5"
  }
}
